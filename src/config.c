
// src/config.c — Définition des couleurs globales
#include <SDL2/SDL.h>
#include "config.h"

// Fond très sombre tirant sur le vert
const SDL_Color COL_BG_DARK = { 0, 20, 0, 255 };
// Texte vert clair lisible
const SDL_Color COL_TEXT    = { 180, 255, 180, 255 };
// Vert plus clair pour la tête de colonne (pluie)
const SDL_Color COL_HEAD    = { 200, 255, 200, 255 };
